rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regra permissiva para o painel de metas
    // Em producao, restrinja por auth ou IP
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
